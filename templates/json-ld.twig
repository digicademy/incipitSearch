<script type="application/ld+json">
    {
	    "@context": "http://schema.org",
        "type": "Dataset",
        "id": "{{ app.request.uri }}",
        "hasPart": [
            {% for catalogEntry in catalogEntries %}
                {% include 'singleresult-json.twig' %}
            {% endfor %}
            {# quick fix: empty element in the end; TODO: loop to check if last element, then do not set a final comma#}
            {
            }
        ]
    }

</script>